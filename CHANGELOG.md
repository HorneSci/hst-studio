# Changelog

Dated by build. This tree is generated by a private release pipeline, so each
entry describes a rebuild, not a merge.

## v2026.08.07a

Same-day fix release, prompted by an external review — six defects, all of the
same shape: the tree's honesty stopped one sentence short.

- Licence section now says plainly that `bin/` is binary-only and why, what
  the Apache grant means over a binary, and how to verify what you received.
- `SHA256SUMS` at the tree root covers `bin/`, `install.sh` and `verify.sh`;
  `./verify.sh` checks it first.
- The tree carries its own public CI (`.github/workflows/verify.yml`), and the
  README's CI claim now points at runs you can see.
- Removed a reference to a test file that only exists in the private estate;
  the Python floor now cites the `pyproject.toml` files that ship.
- Added `CITATION.cff`, `SECURITY.md` (including the build-attestation gap),
  `CONTRIBUTING.md` (honest about the generated-tree model), this file.
- README restructured: the run-your-own-operator section moved up; dated
  errata moved to `ERRATA.md`, verbatim.

## v2026.08.07

First public build of the community tree.
