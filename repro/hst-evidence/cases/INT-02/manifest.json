{
  "schema": "hst-evidence/case-v1",
  "case_id": "INT-02",
  "grade": "GRADE-A",
  "precision": "f64",
  "batch": 1,
  "adversarial": false,
  "why": "integer-exact, drifting dirty set at the measured-supported rho",
  "operator": {
    "source": "suitesparse",
    "group": "HB",
    "name": "bcspwr10",
    "url": "https://suitesparse-collection-website.herokuapp.com/MM/HB/bcspwr10.tar.gz",
    "mtx_sha256": "5011d5518a7d96ab054dadb8f0670fded73385548add1cfa75c43742523c3e33",
    "matrix_market_field": "pattern",
    "matrix_market_symmetry": "symmetric",
    "rows": 5300,
    "cols": 5300,
    "nnz_after_preprocessing": 21842
  },
  "preprocessing": {
    "value_rule": "pattern_int_v1",
    "description": "A `symmetric` Matrix Market file stores one triangle: each off-diagonal (i,j) contributes both (i,j) and (j,i); a diagonal entry contributes once. Stored values are then DISCARDED and replaced by A[i,j] = 1 + ((i + 3j) mod 5) with i,j zero-based, giving exact small integers in 1..5.",
    "duplicate_coordinates": "none present; asserted at generation time"
  },
  "x0_generator": "x0_int_v1",
  "x0_seed": 1102,
  "x0_description": "x0[j][b] = 1 + ((j + 7b) mod 9), zero-based -- integers 1..9",
  "dx": {
    "file": "dx.bin",
    "generator": "dx_v1",
    "seed": 9102,
    "steps": 6,
    "motion": "drift",
    "drift_rho": 0.25,
    "dirty_fraction": 0.02,
    "dirty_columns_per_step": 106,
    "value_domain": "integers in [-4,4]",
    "format": "GVDX0001, little-endian; layout documented in README.md",
    "description": "dx_v1: rng = SplitMix64(seed). k = max(1, round(dirty_fraction * cols)). A column set is drawn by repeatedly taking rng.next() mod cols and rejecting repeats, then sorting ascending. Step 0 draws such a set. motion=frozen keeps it; motion=scattered draws a fresh set every step; motion=drift replaces the r = max(1, round(drift_rho * k)) lowest-indexed members with freshly drawn columns not already retained, then re-sorts. Values are emitted per (position in the dirty set, batch lane), position major."
  },
  "reference": {
    "summation_order": "CSC. Columns ascending; within a column, row indices ascending; y[i*B+b] += A[i,j] * x[j*B+b] applied in exactly that sequence, with b ascending innermost. The delta apply uses the same order restricted to the dirty columns, taken ascending, and accumulates into the running y.",
    "implementations": [
      "reference/reference_spmv.py",
      "reference/reference_spmv.c"
    ]
  },
  "verification": {
    "rel_l2_bound": 0.0,
    "order_independent": true,
    "exactness_argument": "Every A[i,j] is an integer in 1..5 and every x0/dx entry is an integer, so every partial sum is an integer. The largest accumulator any order can reach is max_i sum_j |A[i,j]| * (max|x0| + steps * max|dx|), reported below; it is under 2^53, so every partial sum is exactly representable in f64 and no addition rounds. Bit-equality therefore holds regardless of summation order, compiler, or vectorization. This is the one grade in the set that a correct implementation cannot fail for a benign reason.",
    "max_abs_accumulator": 1617.0,
    "exactly_representable_below": 9007199254740992,
    "observed_rel_l2_between_orders": 0.0,
    "observed_bit_differences_between_orders": 0
  },
  "outputs": {
    "dx.bin": {
      "sha256": "7e5379d0466fc077c85adeffe3905ed96391928ed1f8e52947180ca3ba96b361"
    },
    "y0.f64.bin": {
      "shape": [
        5300,
        1
      ],
      "dtype": "<f8",
      "sha256": "d9fc00198ff0310cdb428a307f32312f2be654ed564f4a8b86523b1260d8245b"
    },
    "y.f64.bin": {
      "shape": [
        6,
        5300,
        1
      ],
      "dtype": "<f8",
      "sha256": "a155e13c056357ac5681ecf535d00a6fef47840efd729d137d641f2e70663885"
    }
  }
}
