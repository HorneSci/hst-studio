{
  "schema": "hst-evidence/case-v1",
  "case_id": "ADV-01",
  "grade": "GRADE-B",
  "precision": "f64",
  "batch": 1,
  "adversarial": true,
  "why": "ADVERSARIAL: 85% of columns dirty, fresh scatter every step. This is the regime a delta approach is supposed to lose in; it is published for that reason.",
  "operator": {
    "source": "suitesparse",
    "group": "Hamm",
    "name": "memplus",
    "url": "https://suitesparse-collection-website.herokuapp.com/MM/Hamm/memplus.tar.gz",
    "mtx_sha256": "7557ed285697c72b4692b41804725194798f9285ef39c0fe3823024214072aa1",
    "matrix_market_field": "real",
    "matrix_market_symmetry": "general",
    "rows": 17758,
    "cols": 17758,
    "nnz_after_preprocessing": 126150
  },
  "preprocessing": {
    "value_rule": "values_f64_v1",
    "description": "A `symmetric` Matrix Market file stores one triangle: each off-diagonal (i,j) contributes both (i,j) and (j,i); a diagonal entry contributes once. Stored values are used as-is; a `pattern` file gives 1.0.",
    "duplicate_coordinates": "none present; asserted at generation time"
  },
  "x0_generator": "x0_f64_v1",
  "x0_seed": 3101,
  "x0_description": "x0[j][b] = 2u-1 where u = (SplitMix64(x0_seed).next() >> 11) * 2^-53, drawn in j-major then b order",
  "dx": {
    "file": "dx.bin",
    "generator": "dx_v1",
    "seed": 7101,
    "steps": 6,
    "motion": "scattered",
    "drift_rho": null,
    "dirty_fraction": 0.85,
    "dirty_columns_per_step": 15094,
    "value_domain": "f64 in [-1,1)",
    "format": "GVDX0001, little-endian; layout documented in README.md",
    "description": "dx_v1: rng = SplitMix64(seed). k = max(1, round(dirty_fraction * cols)). A column set is drawn by repeatedly taking rng.next() mod cols and rejecting repeats, then sorting ascending. Step 0 draws such a set. motion=frozen keeps it; motion=scattered draws a fresh set every step; motion=drift replaces the r = max(1, round(drift_rho * k)) lowest-indexed members with freshly drawn columns not already retained, then re-sorts. Values are emitted per (position in the dirty set, batch lane), position major."
  },
  "reference": {
    "summation_order": "CSC. Columns ascending; within a column, row indices ascending; y[i*B+b] += A[i,j] * x[j*B+b] applied in exactly that sequence, with b ascending innermost. The delta apply uses the same order restricted to the dirty columns, taken ascending, and accumulates into the running y.",
    "implementations": [
      "reference/reference_spmv.py",
      "reference/reference_spmv.c"
    ]
  },
  "verification": {
    "rel_l2_bound": 1e-12,
    "order_independent": false,
    "exactness_argument": "Tolerance grade. The bound is calibrated, not chosen: it is 10x the measured rel_l2 between two legitimate summation orders (CSC and row-major) on this exact case, floored at 1e-12. Any implementation whose result lands inside it agrees with the reference to within the spread that reordering alone produces.",
    "max_abs_accumulator": null,
    "exactly_representable_below": null,
    "observed_rel_l2_between_orders": 4.786282259821132e-16,
    "observed_bit_differences_between_orders": 60783
  },
  "outputs": {
    "dx.bin": {
      "sha256": "b6a616f94cc63786d08809c9c8b07ad947fcc0a3156ef513cef3e7de0366a451"
    },
    "y0.f64.bin": {
      "shape": [
        17758,
        1
      ],
      "dtype": "<f8",
      "sha256": "d36bf64f0cd89d2fce0686b37dfc6f00d6446e45414e0720e1be02037e894b39"
    },
    "y.f64.bin": {
      "shape": [
        6,
        17758,
        1
      ],
      "dtype": "<f8",
      "sha256": "7fdc028244233de4480b1a0885843e43e1e68a1b6f775500f38e037cbf7eabd2"
    }
  }
}
