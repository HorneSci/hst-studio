# Fold arm follow-up

The fold arm is 2.30x faster than the previous release, measured against the
release before it over 5 runs. Nothing here says where it ran or what built
it, so nobody can re-run it.
