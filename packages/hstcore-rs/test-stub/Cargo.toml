[package]
name = "hst-stub"
description = "A recording stub exporting the HST-core Embedded ABI. Test scaffolding only — it computes nothing."
publish = false
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
name = "hst_stub"
crate-type = ["cdylib"]

[dependencies]
